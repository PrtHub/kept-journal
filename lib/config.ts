/**
 * Single source of truth for outbound links.
 *
 * Every "Download on the App Store" affordance on the site resolves here, so
 * the real App Store URL only needs replacing in one place once the app ships.
 */
export const APP_STORE_URL = "https://apps.apple.com/app/kept/id0000000000";

export const SITE_URL = "https://kept.app";
